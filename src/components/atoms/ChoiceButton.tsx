import styled from "styled-components";

const ChoiceButton = () => {
  return (
    <>
      <StyledButton />
    </>
  );
};

export default ChoiceButton;

const StyledButton = styled.button`
  radius: 6px;
  width: 160px;
  height: 160px;
`;
